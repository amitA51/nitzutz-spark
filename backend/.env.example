# Database
DATABASE_URL="file:./dev.db"

# Server Configuration
PORT=5000

# Google Drive API (for summary import)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback

# External AI Provider (server-side configuration)
# Configure an OpenAI-compatible provider (e.g., Hugging Face Router, OpenRouter, Together, etc.)
AI_BASE_URL=https://router.huggingface.co/v1
# Prefer AI_API_KEY; HF_TOKEN is supported as a fallback name
AI_API_KEY=HF_TOKENhf_VRofrqDWodLCeSPaaPQVWVqLnGJRIByqoI
# Default model (can be overridden per request)
AI_MODEL=deepseek-ai/DeepSeek-V3.2-Exp:novita
